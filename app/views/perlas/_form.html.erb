<%= form_for(@perla) do |f| %>
  <% if @perla.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@perla.errors.count, "error") %> prohibited this perla from being saved:</h2>

      <ul>
      <% @perla.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_name %><br />
    <%= f.text_field :first_name %>
  </div>
  <div class="field">
    <%= f.label :last_name %><br />
    <%= f.text_field :last_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
