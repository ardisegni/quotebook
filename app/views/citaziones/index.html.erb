<h1>Perle</h1>

<table>
  <tr>
    <th>Autore</th>
    <th>Data e ora</th>
    <th>Perla</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @citaziones.each do |citazione| %>
  <tr>
    <td><%= citazione.user %></td>
    <td><%= citazione.date_time %></td>
    <td><%= citazione.citazione %></td>
    <td><%= link_to 'Mostra', citazione %></td>
    <td><%= link_to 'Modifica', edit_citazione_path(citazione) %></td>
    <td><%= link_to 'Elimina', citazione, method: :delete, data: { confirm: 'Sei sicuro?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nuova perla', new_citazione_path %>
